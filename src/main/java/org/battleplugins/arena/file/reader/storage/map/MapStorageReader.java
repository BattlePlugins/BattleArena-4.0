package org.battleplugins.arena.file.reader.storage.map;

import org.battleplugins.arena.arena.map.ArenaMap;
import org.battleplugins.arena.file.reader.storage.StorageReader;

/**
 * Storage reader capable of reading {@link ArenaMap}s.
 *
 * @author Redned
 */
public abstract class MapStorageReader<F> implements StorageReader<ArenaMap, F> {

}
